"""Frame-0 anatomical body-angle ranked active-view experiments.

This package is deliberately separate from every historical RL experiment.
The action index in this package is an ordinal angle rank within one sample,
never a camera filename or a global camera order.
"""

__all__ = ["__version__"]
__version__ = "0.1.0"
