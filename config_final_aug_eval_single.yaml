# Reproduce the historical final_aug evaluation protocol from before the
# train/eval text-bank unification: one global-description prototype per class.
base_config: ./config_final_aug.yaml

runtime:
  device: cuda:1

data:
  text:
    prompt_templates: null
