# Stronger preservation run from the actual final_aug best_model checkpoint.
base_config: ./config_final_aug_entropy_anchor_best10.yaml

runtime:
  device: cuda:0

data:
  text:
    cache_output: ./work_dir/final_aug_entropy_anchor_best100/clip_text_action_embeddings.pt

train:
  loss:
    anchor_weight: 100.0

outputs:
  work_dir: ./work_dir/final_aug_entropy_anchor_best100
  run_name: final_best_lambdaH0005_anchor100
