# More aggressive 55/0 entropy minimisation with the same preserved framework.
base_config: ./config_final55_aug_entropy.yaml

runtime:
  device: cuda:0

data:
  text:
    cache_output: ./work_dir/final55_aug_entropy_h5/clip_text_action_embeddings.pt

train:
  loss:
    entropy_weight: 5.0

outputs:
  work_dir: ./work_dir/final55_aug_entropy_h5
  run_name: final55_swa_lambdaH5_anchor100
