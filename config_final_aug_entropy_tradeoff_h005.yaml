# Strong entropy pressure with a stronger checkpoint-preservation anchor.
base_config: ./config_final_aug_entropy_tradeoff_h002.yaml

runtime:
  device: cuda:0

data:
  text:
    cache_output: ./work_dir/final_aug_entropy_tradeoff_h005/clip_text_action_embeddings.pt

train:
  loss:
    entropy_weight: 0.05
    anchor_weight: 100.0

outputs:
  work_dir: ./work_dir/final_aug_entropy_tradeoff_h005
  run_name: final_best_lambdaH0p05_anchor100
